# Contenus runtime (ne pas déployer via Git)
data/site_content.json
data/propositions.json
data/admin_log.json
data/backups/

# Données persistantes de production (CRITIQUE - ne jamais déployer)
data-osons/

# Configuration sécurisée (ne jamais déployer)
forms/recaptcha-config.php
credentials/

# Données admin de production (CRITIQUE - ne jamais écraser)
admin/users.json
admin/logs/
admin/config.php

# Fichiers temporaires et de test
test-cookies.html
TEST_RESULTS.md
tools/test-*.php

# Fichiers système
.DS_Store

